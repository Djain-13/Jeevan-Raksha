[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements-backend.txt"]

[phases.build]
cmds = []

[start]
cmd = "python -m gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
